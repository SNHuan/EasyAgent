---
name: project-intro
description: 先检查项目名，再解释这个项目。
allowed-tools:
  - project_name
---

# Project Intro

先调用 `project_name` 获取项目名。

如果需要更完整的介绍，读取 `references/overview.md`。
最终用 `templates/summary.md` 的结构回答。
